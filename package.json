{
  "name": "finch-app-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "web": "npm run dev --workspace=web",
    "functions": "npm run serve --workspace=functions"
  }
}